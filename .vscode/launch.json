{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Wails (dev tag)",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}",
      "buildFlags": "-tags=dev",
      "cwd": "${workspaceFolder}"
    }
  ]
}
